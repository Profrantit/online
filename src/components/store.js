import React from "react"

export default function Store({ children }) {
  return (
    <div style={{ margin: `3rem auto`, maxWidth: 650, padding: `0 1rem` }}>
      <p>hello from cartjs</p>
    </div>
  )
}
